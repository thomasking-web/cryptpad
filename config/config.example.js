module.exports = {
    // Change httpAddress to '0.0.0.0' to listen on all interfaces
    httpAddress: '0.0.0.0',

    // Keep the HTTP port as 3000
    httpPort: 3000,

    // Set the httpUnsafeOrigin to the URL of the codespace
    httpUnsafeOrigin: 'https://fictional-spork-pwvgxpv56vgfrq75-3000.app.github.dev',

    // The sandbox port should be 3001
    httpSafePort: 3001,
};